<?xml version="1.0" encoding="utf-8"?>
<configuration>
  <!-- Do not add system.net here: ConfigurationManager on .NET Core+ rejects it
       ("Unrecognized configuration section system.net") and MahApps SaveWindowPosition
       then crashes startup with ConfigurationErrorsException. -->
</configuration>
