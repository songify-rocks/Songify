<UserControl
    x:Class="Songify_Slim.UserControls.UcBotResponses"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:properties="clr-namespace:Songify_Slim.Properties"
    d:DesignWidth="800"
    Loaded="UserControl_Loaded"
    mc:Ignorable="d">

    <Grid d:Background="White">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width=".5*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <StackPanel Grid.ColumnSpan="2" Orientation="Vertical">

            <!--#region Song Request Related Responses-->
            <TextBlock
                Margin="0,16,0,6"
                d:Text="{DynamicResource window_botresponses_song_request_responses}"
                FontSize="18"
                FontWeight="Bold"
                Text="{DynamicResource window_botresponses_song_request_responses}" />
            <Border
                Margin="0,12,0,6"
                VerticalAlignment="Bottom"
                BorderBrush="{DynamicResource MahApps.Brushes.Accent3}"
                BorderThickness="1" />

            <Border
                MinWidth="60"
                Margin="0,6"
                Padding="6,6,6,8"
                Background="{DynamicResource MahApps.Brushes.Accent}"
                CornerRadius="8">
                <StackPanel Orientation="Vertical">
                    <TextBlock
                        Margin="0,0,0,6"
                        HorizontalAlignment="Left"
                        VerticalAlignment="Center"
                        d:Text="{DynamicResource window_botresponses_success}"
                        FontSize="14"
                        Foreground="{DynamicResource MahApps.Brushes.IdealForeground}"
                        Text="{DynamicResource window_botresponses_success}"
                        TextWrapping="Wrap" />
                    <TextBox
                        x:Name="TbSrReward"
                        MinHeight="23"
                        VerticalAlignment="Center"
                        AcceptsReturn="True"
                        GotFocus="Tb__GotFocus"
                        LostFocus="Tb_LostFocus"
                        TextChanged="Tb_SrReward_TextChanged"
                        TextWrapping="Wrap"
                        ToolTip="{DynamicResource ParamTooltip}" />
                </StackPanel>
            </Border>
            <Border
                Margin="0,12,0,6"
                VerticalAlignment="Bottom"
                BorderBrush="{DynamicResource MahApps.Brushes.Accent3}"
                BorderThickness="1" />

            <Border
                MinWidth="60"
                Margin="0,6"
                Padding="6,6,6,8"
                Background="{DynamicResource MahApps.Brushes.Accent}"
                CornerRadius="8">
                <StackPanel Orientation="Vertical">
                    <TextBlock
                        Margin="0,0,0,6"
                        HorizontalAlignment="Left"
                        VerticalAlignment="Center"
                        d:Text="{DynamicResource window_botresponses_song_in_queue}"
                        FontSize="14"
                        Foreground="{DynamicResource MahApps.Brushes.IdealForeground}"
                        Text="{DynamicResource window_botresponses_song_in_queue}"
                        TextWrapping="Wrap" />
                    <TextBox
                        x:Name="TbSongInQueue"
                        MinHeight="23"
                        VerticalAlignment="Center"
                        AcceptsReturn="True"
                        GotFocus="Tb__GotFocus"
                        LostFocus="Tb_LostFocus"
                        TextChanged="Tb_SongInQueue_TextChanged"
                        TextWrapping="Wrap"
                        ToolTip="{DynamicResource ParamTooltip}" />
                </StackPanel>
            </Border>
            <Border
                Margin="0,12,0,6"
                VerticalAlignment="Bottom"
                BorderBrush="{DynamicResource MahApps.Brushes.Accent3}"
                BorderThickness="1" />

            <Border
                MinWidth="60"
                Margin="0,6"
                Padding="6,6,6,8"
                Background="{DynamicResource MahApps.Brushes.Accent}"
                CornerRadius="8">
                <StackPanel Orientation="Vertical">
                    <TextBlock
                        Margin="0,0,0,6"
                        HorizontalAlignment="Left"
                        VerticalAlignment="Center"
                        d:Text="{DynamicResource window_botresponses_max_songs}"
                        FontSize="14"
                        Foreground="{DynamicResource MahApps.Brushes.IdealForeground}"
                        Text="{DynamicResource window_botresponses_max_songs}"
                        TextWrapping="Wrap" />
                    <TextBox
                        x:Name="TbMaxSongs"
                        MinHeight="23"
                        VerticalAlignment="Center"
                        AcceptsReturn="True"
                        GotFocus="Tb__GotFocus"
                        LostFocus="Tb_LostFocus"
                        TextChanged="Tb_MaxSongs_TextChanged"
                        TextWrapping="Wrap"
                        ToolTip="{DynamicResource ParamTooltip}" />
                </StackPanel>
            </Border>

            <Border
                Margin="0,12,0,6"
                VerticalAlignment="Bottom"
                BorderBrush="{DynamicResource MahApps.Brushes.Accent3}"
                BorderThickness="1" />

            <Border
                MinWidth="60"
                Margin="0,6"
                Padding="6,6,6,8"
                Background="{DynamicResource MahApps.Brushes.Accent}"
                CornerRadius="8">
                <StackPanel Orientation="Vertical">
                    <TextBlock
                        Margin="0,0,0,6"
                        HorizontalAlignment="Left"
                        VerticalAlignment="Center"
                        d:Text="{DynamicResource window_botresponses_song_too_long}"
                        FontSize="14"
                        Foreground="{DynamicResource MahApps.Brushes.IdealForeground}"
                        Text="{DynamicResource window_botresponses_song_too_long}"
                        TextWrapping="Wrap" />
                    <TextBox
                        x:Name="TbMaxLength"
                        MinHeight="23"
                        VerticalAlignment="Center"
                        AcceptsReturn="True"
                        GotFocus="Tb__GotFocus"
                        LostFocus="Tb_LostFocus"
                        TextChanged="Tb_MaxLength_TextChanged"
                        TextWrapping="Wrap"
                        ToolTip="{DynamicResource ParamTooltip}" />
                </StackPanel>
            </Border>
            <Border
                Margin="0,12,0,6"
                VerticalAlignment="Bottom"
                BorderBrush="{DynamicResource MahApps.Brushes.Accent3}"
                BorderThickness="1" />

            <Border
                MinWidth="60"
                Margin="0,6"
                Padding="6,6,6,8"
                Background="{DynamicResource MahApps.Brushes.Accent}"
                CornerRadius="8">
                <StackPanel Orientation="Vertical">
                    <TextBlock
                        Margin="0,0,0,6"
                        HorizontalAlignment="Left"
                        VerticalAlignment="Center"
                        d:Text="{DynamicResource window_botresponses_user_level_too_low_command}"
                        FontSize="14"
                        Foreground="{DynamicResource MahApps.Brushes.IdealForeground}"
                        Text="{DynamicResource window_botresponses_user_level_too_low_command}"
                        TextWrapping="Wrap" />
                    <TextBox
                        x:Name="TbSrCommandUSerlevelTooLow"
                        MinHeight="23"
                        VerticalAlignment="Center"
                        AcceptsReturn="True"
                        GotFocus="Tb__GotFocus"
                        LostFocus="Tb_LostFocus"
                        TextChanged="Tb_UserLevelTooLowCommand_TextChanged"
                        TextWrapping="Wrap"
                        ToolTip="{DynamicResource ParamTooltip}" />
                </StackPanel>
            </Border>
            <Border
                Margin="0,12,0,6"
                VerticalAlignment="Bottom"
                BorderBrush="{DynamicResource MahApps.Brushes.Accent3}"
                BorderThickness="1" />

            <Border
                MinWidth="60"
                Margin="0,6"
                Padding="6,6,6,8"
                Background="{DynamicResource MahApps.Brushes.Accent}"
                CornerRadius="8">
                <StackPanel Orientation="Vertical">
                    <TextBlock
                        Margin="0,0,0,6"
                        HorizontalAlignment="Left"
                        VerticalAlignment="Center"
                        d:Text="{DynamicResource window_botresponses_user_level_too_low_reward}"
                        FontSize="14"
                        Foreground="{DynamicResource MahApps.Brushes.IdealForeground}"
                        Text="{DynamicResource window_botresponses_user_level_too_low_reward}"
                        TextWrapping="Wrap" />

                    <TextBox
                        x:Name="TbSrRewardUSerlevelTooLow"
                        MinHeight="23"
                        VerticalAlignment="Center"
                        AcceptsReturn="True"
                        GotFocus="Tb__GotFocus"
                        LostFocus="Tb_LostFocus"
                        TextChanged="Tb_UserLevelTooLowReward_TextChanged"
                        TextWrapping="Wrap"
                        ToolTip="{DynamicResource ParamTooltip}" />
                </StackPanel>
            </Border>
            <Border
                Margin="0,12,0,6"
                VerticalAlignment="Bottom"
                BorderBrush="{DynamicResource MahApps.Brushes.Accent3}"
                BorderThickness="1" />

            <Border
                MinWidth="60"
                Margin="0,6"
                Padding="6,6,6,8"
                Background="{DynamicResource MahApps.Brushes.Accent}"
                CornerRadius="8">
                <StackPanel Orientation="Vertical">
                    <TextBlock
                        Margin="0,0,0,6"
                        HorizontalAlignment="Left"
                        VerticalAlignment="Center"
                        d:Text="{DynamicResource window_botresponses_artist_blocked}"
                        FontSize="14"
                        Foreground="{DynamicResource MahApps.Brushes.IdealForeground}"
                        Text="{DynamicResource window_botresponses_artist_blocked}"
                        TextWrapping="Wrap" />
                    <TextBox
                        x:Name="TbArtistBlocked"
                        MinHeight="23"
                        VerticalAlignment="Center"
                        AcceptsReturn="True"
                        GotFocus="Tb__GotFocus"
                        LostFocus="Tb_LostFocus"
                        TextChanged="Tb_ArtistBlocked_TextChanged"
                        TextWrapping="Wrap"
                        ToolTip="{DynamicResource ParamTooltip}" />
                </StackPanel>
            </Border>
            <Border
                Margin="0,12,0,6"
                VerticalAlignment="Bottom"
                BorderBrush="{DynamicResource MahApps.Brushes.Accent3}"
                BorderThickness="1" />

            <Border
                MinWidth="60"
                Margin="0,6"
                Padding="6,6,6,8"
                Background="{DynamicResource MahApps.Brushes.Accent}"
                CornerRadius="8">
                <StackPanel Orientation="Vertical">
                    <TextBlock
                        Margin="0,0,0,6"
                        HorizontalAlignment="Left"
                        VerticalAlignment="Center"
                        d:Text="{DynamicResource window_botresponses_song_blocked}"
                        FontSize="14"
                        Foreground="{DynamicResource MahApps.Brushes.IdealForeground}"
                        Text="{DynamicResource window_botresponses_song_blocked}"
                        TextWrapping="Wrap" />
                    <TextBox
                        x:Name="TbSongBlocked"
                        MinHeight="23"
                        VerticalAlignment="Center"
                        AcceptsReturn="True"
                        GotFocus="Tb__GotFocus"
                        LostFocus="Tb_LostFocus"
                        TextChanged="Tb_SongBlocked_TextChanged"
                        TextWrapping="Wrap"
                        ToolTip="{DynamicResource ParamTooltip}" />
                </StackPanel>
            </Border>
            <Border
                Margin="0,12,0,6"
                VerticalAlignment="Bottom"
                BorderBrush="{DynamicResource MahApps.Brushes.Accent3}"
                BorderThickness="1" />

            <Border
                MinWidth="60"
                Margin="0,6"
                Padding="6,6,6,8"
                Background="{DynamicResource MahApps.Brushes.Accent}"
                CornerRadius="8">
                <StackPanel Orientation="Vertical">
                    <TextBlock
                        Margin="0,0,0,6"
                        HorizontalAlignment="Left"
                        VerticalAlignment="Center"
                        d:Text="{DynamicResource window_botresponses_explicit_songs}"
                        FontSize="14"
                        Foreground="{DynamicResource MahApps.Brushes.IdealForeground}"
                        Text="{DynamicResource window_botresponses_explicit_songs}"
                        TextWrapping="Wrap" />
                    <TextBox
                        x:Name="TbExplicit"
                        MinHeight="27"
                        VerticalAlignment="Center"
                        AcceptsReturn="True"
                        GotFocus="Tb__GotFocus"
                        LostFocus="Tb_LostFocus"
                        TextChanged="TbExplicit_TextChanged"
                        TextWrapping="Wrap"
                        ToolTip="{DynamicResource ParamTooltip}" />
                </StackPanel>
            </Border>
            <Border
                Margin="0,12,0,6"
                VerticalAlignment="Bottom"
                BorderBrush="{DynamicResource MahApps.Brushes.Accent3}"
                BorderThickness="1" />

            <Border
                MinWidth="60"
                Margin="0,6"
                Padding="6,6,6,8"
                Background="{DynamicResource MahApps.Brushes.Accent}"
                CornerRadius="8">
                <StackPanel Orientation="Vertical">
                    <TextBlock
                        Margin="0,0,0,6"
                        HorizontalAlignment="Left"
                        VerticalAlignment="Center"
                        d:Text="{DynamicResource window_botresponses_command_on_cooldown}"
                        FontSize="14"
                        Foreground="{DynamicResource MahApps.Brushes.IdealForeground}"
                        Text="{DynamicResource window_botresponses_command_on_cooldown}"
                        TextWrapping="Wrap" />
                    <TextBox
                        x:Name="TbSrCooldown"
                        MinHeight="27"
                        VerticalAlignment="Center"
                        AcceptsReturn="True"
                        GotFocus="Tb__GotFocus"
                        LostFocus="Tb_LostFocus"
                        TextChanged="TbSrCooldown_TextChanged"
                        TextWrapping="Wrap"
                        ToolTip="{DynamicResource ParamTooltip}" />
                </StackPanel>
            </Border>
            <Border
                Margin="0,12,0,6"
                VerticalAlignment="Bottom"
                BorderBrush="{DynamicResource MahApps.Brushes.Accent3}"
                BorderThickness="1" />

            <Border
                MinWidth="60"
                Margin="0,6"
                Padding="6,6,6,8"
                Background="{DynamicResource MahApps.Brushes.Accent}"
                CornerRadius="8">
                <StackPanel Orientation="Vertical">
                    <TextBlock
                        Margin="0,0,0,6"
                        HorizontalAlignment="Left"
                        VerticalAlignment="Center"
                        d:Text="{DynamicResource window_botresponses_command_on_user_cooldown}"
                        FontSize="14"
                        Foreground="{DynamicResource MahApps.Brushes.IdealForeground}"
                        Text="{DynamicResource window_botresponses_command_on_user_cooldown}"
                        TextWrapping="Wrap" />
                    <TextBox
                        x:Name="TbSrUserCooldown"
                        MinHeight="27"
                        VerticalAlignment="Center"
                        AcceptsReturn="True"
                        GotFocus="Tb__GotFocus"
                        LostFocus="Tb_LostFocus"
                        TextChanged="TbSrUserCooldown_OnTextChanged"
                        TextWrapping="Wrap"
                        ToolTip="{DynamicResource ParamTooltip}" />
                </StackPanel>
            </Border>
            <Border
                Margin="0,12,0,6"
                VerticalAlignment="Bottom"
                BorderBrush="{DynamicResource MahApps.Brushes.Accent3}"
                BorderThickness="1" />

            <!--<Border
                MinWidth="60"
                Margin="0,6"
                Padding="6,6,6,8"
                Background="{DynamicResource MahApps.Brushes.Accent}"
                CornerRadius="8">
                <StackPanel Orientation="Vertical">
                    <TextBlock
                        Margin="0,0,0,6"
                        HorizontalAlignment="Left"
                        VerticalAlignment="Center"
                        FontSize="14"
                        Foreground="{DynamicResource MahApps.Brushes.IdealForeground}"
                        Text="{DynamicResource window_botresponses_mod_skip}" d:Text="{DynamicResource window_botresponses_mod_skip}"
                        TextWrapping="Wrap" />
                    <TextBox
                        x:Name="TbModSkip"
                        MinHeight="23"
                        VerticalAlignment="Center"
                        AcceptsReturn="True"
                        GotFocus="Tb__GotFocus"
                        LostFocus="Tb_LostFocus"
                        TextChanged="Tb_ModSkip_TextChanged"
                        TextWrapping="Wrap"
                        ToolTip="{DynamicResource ParamTooltip}" />
                </StackPanel>
            </Border>
            <Border
                Margin="0,12,0,6"
                VerticalAlignment="Bottom"
                BorderBrush="{DynamicResource MahApps.Brushes.Accent3}"
                BorderThickness="1" />-->

            <!--<Border
                MinWidth="60"
                Margin="0,6"
                Padding="6,6,6,8"
                Background="{DynamicResource MahApps.Brushes.Accent}"
                CornerRadius="8">
                <StackPanel Orientation="Vertical">
                    <TextBlock
                        Margin="0,0,0,6"
                        HorizontalAlignment="Left"
                        VerticalAlignment="Center"
                        FontSize="14"
                        Foreground="{DynamicResource MahApps.Brushes.IdealForeground}"
                        Text="{DynamicResource window_botresponses_vote_skip}" d:Text="{DynamicResource window_botresponses_vote_skip}"
                        TextWrapping="Wrap" />
                    <TextBox
                        x:Name="TbVoteSkip"
                        MinHeight="23"
                        VerticalAlignment="Center"
                        AcceptsReturn="True"
                        GotFocus="Tb__GotFocus"
                        LostFocus="Tb_LostFocus"
                        TextChanged="Tb_VoteSkip_TextChanged"
                        TextWrapping="Wrap"
                        ToolTip="{DynamicResource ParamTooltip}" />
                </StackPanel>
            </Border>
            <Border
                Margin="0,12,0,6"
                VerticalAlignment="Bottom"
                BorderBrush="{DynamicResource MahApps.Brushes.Accent3}"
                BorderThickness="1" />-->

            <Border
                MinWidth="60"
                Margin="0,6"
                Padding="6,6,6,8"
                Background="{DynamicResource MahApps.Brushes.Accent}"
                CornerRadius="8">
                <StackPanel Orientation="Vertical">
                    <TextBlock
                        Margin="0,0,0,6"
                        HorizontalAlignment="Left"
                        VerticalAlignment="Center"
                        d:Text="{DynamicResource window_botresponses_song_not_in_playlist}"
                        FontSize="14"
                        Foreground="{DynamicResource MahApps.Brushes.IdealForeground}"
                        Text="{DynamicResource window_botresponses_song_not_in_playlist}"
                        TextWrapping="Wrap" />
                    <TextBox
                        x:Name="TbNotFoundInPlaylist"
                        MinHeight="27"
                        VerticalAlignment="Center"
                        AcceptsReturn="True"
                        GotFocus="Tb__GotFocus"
                        LostFocus="Tb_LostFocus"
                        TextChanged="TbNotFoundInPlaylist_TextChanged"
                        TextWrapping="Wrap"
                        ToolTip="{DynamicResource ParamTooltip}" />
                </StackPanel>
            </Border>

            <Border
                Margin="0,12,0,6"
                VerticalAlignment="Bottom"
                BorderBrush="{DynamicResource MahApps.Brushes.Accent3}"
                BorderThickness="1" />

            <Border
                MinWidth="60"
                Margin="0,6"
                Padding="6,6,6,8"
                Background="{DynamicResource MahApps.Brushes.Accent}"
                CornerRadius="8">
                <StackPanel Orientation="Vertical">
                    <TextBlock
                        Margin="0,0,0,6"
                        HorizontalAlignment="Left"
                        VerticalAlignment="Center"
                        d:Text="{DynamicResource window_botresponses_fetch_error}"
                        FontSize="14"
                        Foreground="{DynamicResource MahApps.Brushes.IdealForeground}"
                        Text="{DynamicResource window_botresponses_fetch_error}"
                        TextWrapping="Wrap" />
                    <TextBox
                        x:Name="TbError"
                        MinHeight="23"
                        VerticalAlignment="Center"
                        AcceptsReturn="True"
                        GotFocus="Tb__GotFocus"
                        LostFocus="Tb_LostFocus"
                        TextChanged="Tb_Error_TextChanged"
                        TextWrapping="Wrap"
                        ToolTip="{DynamicResource ParamTooltip}" />
                </StackPanel>
            </Border>
            <Border
                Margin="0,12,0,6"
                VerticalAlignment="Bottom"
                BorderBrush="{DynamicResource MahApps.Brushes.Accent3}"
                BorderThickness="1" />

            <Border
                MinWidth="60"
                Margin="0,6"
                Padding="6,6,6,8"
                Background="{DynamicResource MahApps.Brushes.Accent}"
                CornerRadius="8">
                <StackPanel Orientation="Vertical">
                    <TextBlock
                        Margin="0,0,0,6"
                        HorizontalAlignment="Left"
                        VerticalAlignment="Center"
                        d:Text="{DynamicResource window_botresponses_no_song}"
                        FontSize="14"
                        Foreground="{DynamicResource MahApps.Brushes.IdealForeground}"
                        Text="{DynamicResource window_botresponses_no_song}"
                        TextWrapping="Wrap" />
                    <TextBox
                        x:Name="TbNoSong"
                        MinHeight="23"
                        VerticalAlignment="Center"
                        AcceptsReturn="True"
                        GotFocus="Tb__GotFocus"
                        LostFocus="Tb_LostFocus"
                        TextChanged="Tb_NoSong_TextChanged"
                        TextWrapping="Wrap"
                        ToolTip="{DynamicResource ParamTooltip}" />
                </StackPanel>
            </Border>
            <Border
                Margin="0,12,0,6"
                VerticalAlignment="Bottom"
                BorderBrush="{DynamicResource MahApps.Brushes.Accent3}"
                BorderThickness="1" />

            <Border
                MinWidth="60"
                Margin="0,6"
                Padding="6,6,6,8"
                Background="{DynamicResource MahApps.Brushes.Accent}"
                CornerRadius="8">
                <StackPanel Orientation="Vertical">
                    <TextBlock
                        Margin="0,0,0,6"
                        HorizontalAlignment="Left"
                        VerticalAlignment="Center"
                        d:Text="{DynamicResource window_botresponses_no_track_found}"
                        FontSize="14"
                        Foreground="{DynamicResource MahApps.Brushes.IdealForeground}"
                        Text="{DynamicResource window_botresponses_no_track_found}"
                        TextWrapping="Wrap" />
                    <TextBox
                        x:Name="TbnoTrackFound"
                        MinHeight="27"
                        VerticalAlignment="Center"
                        AcceptsReturn="True"
                        GotFocus="Tb__GotFocus"
                        LostFocus="Tb_LostFocus"
                        TextChanged="TbnoTrackFound_TextChanged"
                        TextWrapping="Wrap"
                        ToolTip="{DynamicResource ParamTooltip}" />
                </StackPanel>
            </Border>

            <Border
                Margin="0,12,0,6"
                VerticalAlignment="Bottom"
                BorderBrush="{DynamicResource MahApps.Brushes.Accent3}"
                BorderThickness="1" />

            <Border
                MinWidth="60"
                Margin="0,6"
                Padding="6,6,6,8"
                Background="{DynamicResource MahApps.Brushes.Accent}"
                CornerRadius="8">
                <StackPanel Orientation="Vertical">
                    <TextBlock
                        Margin="0,0,0,6"
                        HorizontalAlignment="Left"
                        VerticalAlignment="Center"
                        d:Text="{DynamicResource window_botresponses_refund}"
                        FontSize="14"
                        Foreground="{DynamicResource MahApps.Brushes.IdealForeground}"
                        Text="{DynamicResource window_botresponses_refund}"
                        TextWrapping="Wrap" />
                    <TextBox
                        x:Name="TbRefund"
                        MinHeight="23"
                        VerticalAlignment="Center"
                        AcceptsReturn="True"
                        GotFocus="Tb__GotFocus"
                        LostFocus="Tb_LostFocus"
                        TextChanged="Tb_Refund_OnTextChanged"
                        TextWrapping="Wrap"
                        ToolTip="{DynamicResource ParamTooltip}" />
                </StackPanel>
            </Border>
            <!--#endregion-->
        </StackPanel>
    </Grid>
</UserControl>