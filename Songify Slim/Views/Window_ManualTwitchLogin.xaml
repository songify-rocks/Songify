<controls:MetroWindow x:Class="Songify_Slim.Views.WindowManualTwitchLogin"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Songify_Slim.Views"
        xmlns:controls="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
        mc:Ignorable="d"
        Title="{DynamicResource window_manualtwitch_title}" Height="275" Width="553" WindowStartupLocation="CenterOwner">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <TextBlock TextWrapping="Wrap" Grid.ColumnSpan="2" Text="{DynamicResource window_manualtwitch_instructions}" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="10"/>

        <TextBlock TextWrapping="Wrap" Grid.Row="1" Grid.Column="0" Margin="10,0" Text="{DynamicResource window_manualtwitch_step1}" VerticalAlignment="Center"/>
        <Button Grid.Row="1" Grid.Column="1" Content="{DynamicResource window_manualtwitch_open_login}" Margin="10,0" Click="Button_OpenTwitchLoginPage_Click" Height="30"/>

        <TextBlock TextWrapping="Wrap" Grid.Row="2" Grid.Column="0" Margin="10,0" DockPanel.Dock="Left" Text="{DynamicResource window_manualtwitch_step2}" VerticalAlignment="Center"/>

        <PasswordBox Grid.Row="2" Grid.Column="1" x:Name="TextBoxTwitchCode" Margin="10,0" Height="30"/>

        <TextBlock TextWrapping="Wrap" Grid.Row="3" Grid.Column="0"  Margin="10,0" Text="{DynamicResource window_manualtwitch_step3}" VerticalAlignment="Center"/>
        <Button Grid.Row="3" Grid.Column="1" Height="30" Content="{DynamicResource window_manualtwitch_link}" Margin="10,0" Click="Button_LinkAccounts_Click"></Button>

    </Grid>
</controls:MetroWindow>
