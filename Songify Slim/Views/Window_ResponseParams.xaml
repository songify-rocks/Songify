<Window
    x:Class="Songify_Slim.Views.Window_ResponseParams"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:Songify_Slim.Views"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    Title="{DynamicResource window_responseparams_title}"
    Width="400"
    Height="480"
    Loaded="Window_Loaded"
    LocationChanged="Window_ResponseParams_OnLocationChanged"
    ResizeMode="NoResize"
    WindowStartupLocation="Manual"
    mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />

            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <ui:TextBox
            x:Name="TxtSearch"
            Grid.Row="0"
            Margin="8,8,8,0"
            VerticalContentAlignment="Center"
            PlaceholderText="{DynamicResource window_responseparams_search_watermark}"
            TextChanged="TxtSearch_TextChanged" />
        <Label
            Grid.Row="1"
            Margin="6,6"
            Padding="0"
            Content="{DynamicResource window_responseparams_hint_click_to_copy}"
            FontSize="10" />
        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
            <StackPanel x:Name="PnlParams" Orientation="Vertical" />
        </ScrollViewer>
    </Grid>
</Window>
