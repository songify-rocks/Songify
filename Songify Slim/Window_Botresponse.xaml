<controls:MetroWindow x:Class="Songify_Slim.Window_Botresponse"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Songify_Slim.Properties"
        mc:Ignorable="d"
        xmlns:controls="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"        
        Title="Bot Responses" Height="312.333" Width="629" Loaded="MetroWindow_Loaded">
    <Grid>
        <Label Content="{x:Static local:Resources.brw_Header}" HorizontalAlignment="Left" Margin="10,10,0,0" VerticalAlignment="Top"/>
        <Label Content="{x:Static local:Resources.brw_ArtistBlocked}" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="10,41,0,0"/>
        <Label Content="{x:Static local:Resources.brw_SongInQueue}" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="10,72,0,0"/>
        <Label Content="{x:Static local:Resources.brw_MaxSongs}" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="10,103,0,0"/>
        <Label Content="{x:Static local:Resources.brw_SongTooLong}" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="10,134,0,0"/>
        <Label Content="{x:Static local:Resources.brw_FetchError}" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="10,165,0,0"/>
        <Label Content="{x:Static local:Resources.brw_Success}" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="10,196,0,0"/>
        <TextBox x:Name="tb_ArtistBlocked" Height="23" VerticalAlignment="Top" Margin="154,41,15,0" TextChanged="tb_ArtistBlocked_TextChanged" ToolTip="{DynamicResource ParamTooltip}" GotFocus="tb__GotFocus"/>
        <TextBox x:Name="tb_SongInQueue" Height="23" VerticalAlignment="Top" Margin="154,72,15,0" TextChanged="tb_SongInQueue_TextChanged" ToolTip="{DynamicResource ParamTooltip}" GotFocus="tb__GotFocus"/>
        <TextBox x:Name="tb_MaxSongs" Height="23" VerticalAlignment="Top" Margin="154,103,15,0" TextChanged="tb_MaxSongs_TextChanged" ToolTip="{DynamicResource ParamTooltip}" GotFocus="tb__GotFocus"/>
        <TextBox x:Name="tb_MaxLength" Height="23" VerticalAlignment="Top" Margin="154,134,15,0" TextChanged="tb_MaxLength_TextChanged" ToolTip="{DynamicResource ParamTooltip}" GotFocus="tb__GotFocus"/>
        <TextBox x:Name="tb_Error" Height="23" VerticalAlignment="Top" Margin="154,165,15,0" TextChanged="tb_Error_TextChanged" ToolTip="{DynamicResource ParamTooltip}" GotFocus="tb__GotFocus"/>
        <TextBox x:Name="tb_Success" Height="23" VerticalAlignment="Top" Margin="154,196,15,0" TextChanged="tb_Success_TextChanged" ToolTip="{DynamicResource ParamTooltip}" GotFocus="tb__GotFocus"/>
        <TextBlock x:Name="lbl_Preview" VerticalAlignment="Top" Margin="10,237,15,0" Height="34" TextWrapping="Wrap"/>

    </Grid>
</controls:MetroWindow>
